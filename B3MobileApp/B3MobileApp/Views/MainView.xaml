<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:B3MobileApp.ViewModels;assembly=B3MobileApp"
             x:Class="B3MobileApp.Views.MainView"
             Title="Activity">
  
  <StackLayout Orientation="Vertical" VerticalOptions="Center" Padding="50">
    <Label Text="Please log in with your credentials:" FontSize="18" />
    
    <Label Text="Login:" VerticalOptions="Center" />
    <Entry Text="{Binding Login}" />
    
    <Label Text="Password:" />
    <Entry Text="{Binding Password}" />
    
    <Button Text="Log in" Command="{Binding LogInCommand}" />
  </StackLayout>

</ContentPage>